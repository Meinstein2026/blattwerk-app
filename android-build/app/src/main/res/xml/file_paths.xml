<?xml version="1.0" encoding="utf-8"?>
<paths>
    <external-files-path name="pictures" path="Pictures" />
    <!-- Heruntergeladene Update-APK für den Paket-Installer
         (interner Update-Host, siehe local.properties BW_HOME_URL) -->
    <cache-path name="updates" path="updates" />
</paths>
